npm init creer dans le current dir un package.json qui est le file de conf pour un projet en nodejs
npx hardhat compile
npx hardhat run scripts/deploy.js --network localhost
l'ABI (application vinary interface) permet d'interagir avec le contrat, ca definit  les methodes et les structures du contrat
le fournisseur est un servoce/module qui permet a ma dApp de communiquer avec les nodes de la blockchain (http ac infura et local ac hardhat(fork))
- configurer le .env pour store l'id infra, le num du bloc, l'adresse du contrat


